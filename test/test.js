const assert = require('assert')
const urlShortener = require('../url-shortener')

describe('UrlShortener', () => {
  describe('shorten', () => {
    it('returns a valid short url', () => {
      const longUrl = 'https://www.t-mobile.com'
    })
  })
})
